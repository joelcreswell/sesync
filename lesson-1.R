## Configure git

system("git config --global user.name 'Joel Creswell'")
system("git config --global user.email 'creswell.joel@epa.gov'")

## Set a new origin URL

system('git remote set-url origin https://github.com/joelcreswell/sesync.git')